const defaultResult = 0;
let currentResult = defaultResult;

currentResult = (currentResult + 10) / 2 * 4 - 1;

let calculationLog = `(${currentResult} + 10) / 2 * 4 - 1`

outputResult(currentResult, calculationLog)
